[package]
name = "frontend"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.12.5", features = ["blocking"]}
yew = { version = "0.21.0", features = ["csr"]}
wasm-bindgen-futures = "0.4.42"
